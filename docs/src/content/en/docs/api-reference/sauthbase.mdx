---
title: sauthbase Class
---

# sauthbase Class

`sauthbase` is the main class of the SDK and provides the interface for all authentication and encryption functionalities.

## Overview

- Operates as a singleton
- Use `init()` to initialize, and `use()` to access the instance
- Requires `secretKey` and `redirect_url` as arguments during initialization

## Initialization

```ts filename="TypeScript" showLineNumbers twoslash
// @noErrors
import { sauthbase } from "sauthbase";
// ---cut---
const sab = sauthbase.init({ secretKey: "...", redirect_url: "..." });
```

## Usage

```ts filename="TypeScript" showLineNumbers twoslash
// @noErrors
import { sauthbase } from "sauthbase";
// ---cut---
const sab = sauthbase.use();
```

## Main Method List

- [init()](./methods/init)
- [use()](./methods/use)
- [isReady()](./methods/isReady)
- [generationAuthPageUrl()](./methods/generationAuthPageUrl)
- [getUser()](./methods/getUser)
- [calculateHmac()](./methods/calculateHmac)
- [encrypt()](./methods/encrypt)
- [decrypt()](./methods/decrypt)
- [generateSecureKey()](./methods/generateSecureKey)
- [generateEncryptedToken()](./methods/generateEncryptedToken)
- [verifySession()](./methods/verifySession)
- [extractUserWithVerify()](./methods/extractUserWithVerify)
- [extractUserUnsafe()](./methods/extractUserUnsafe)

For detailed information about each method, refer to the [methods](./methods) section.